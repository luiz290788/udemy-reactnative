'use strict';

import React, {
  Text,
  View,
  Component,
  AppRegistry
} from 'react-native';

class StopWatch extends Component {
  render() {
    return (

    );
  }
}

AppRegistry.registerComponent('stopwatch', () => StopWatch);
